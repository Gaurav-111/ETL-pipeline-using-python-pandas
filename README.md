# ETL pipeline project

### About Project

In this project i build a basic pipeline with help of python **pandas** library and **postgresql** database

**->** use pandas **injest** data form source csv file called **marketing.csv** this file contains all column about marketing campaign,  
       then apply **transformation** on injested data **handlind data types , mapping values ,fix string values** and seprete the data into three diffrent dataframs called "client","campaign",economics"  
       and at end load data into three database table and three csv files
      

